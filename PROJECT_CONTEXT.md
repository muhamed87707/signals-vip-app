# 📋 ملف سياق المشروع الشامل - Abu Al-Dahab VIP Signals Platform

## 🎯 نظرة عامة على المشروع

هذا المشروع هو **منصة توصيات تداول VIP** متكاملة تقدم توصيات الذهب (XAUUSD) والفوركس للمشتركين. المنصة مبنية باستخدام **Next.js 15** مع **MongoDB** كقاعدة بيانات، وتدعم النشر المتعدد على **الموقع** و**تليجرام** و**تويتر (X)**.

### الميزات الرئيسية:
- ✅ نشر متعدد المنصات (موقع + تليجرام + تويتر)
- ✅ نظام VIP مع تمويه الصور للتوصيات الحصرية
- ✅ دعم ثنائي اللغة (عربي/إنجليزي) مع RTL
- ✅ لوحة تحكم إدمن شاملة (4 تبويبات)
- ✅ نظام إحالة مع مكافآت
- ✅ توليد منشورات بالذكاء الاصطناعي (Gemini AI)
- ✅ حاسبة حجم العقد (Lot Size Calculator)
- ✅ سجل الأداء (Track Record)
- ✅ مدونة تعليمية
- ✅ مركز تحليل السوق (Economic Calendar, COT Report, Bank Analysis)

---

## 🏗️ هيكل المشروع الكامل

```
├── app/                              # Next.js App Router
│   ├── page.js                       # الصفحة الرئيسية (Landing Page) - 742 سطر
│   ├── layout.js                     # Layout الرئيسي مع LanguageProvider
│   ├── globals.css                   # الأنماط العامة
│   ├── favicon.ico
│   │
│   ├── admin/page.js                 # لوحة تحكم الإدمن (2748 سطر)
│   ├── signals/page.js               # صفحة التوصيات للمستخدمين
│   ├── track-record/page.js          # سجل الأداء
│   ├── blog/page.js                  # المدونة التعليمية
│   ├── legal/page.js                 # الصفحات القانونية (Privacy, Terms, Risk)
│   │
│   ├── api/                          # API Routes
│   │   ├── signals/route.js          # CRUD للتوصيات + نشر تليجرام/تويتر
│   │   ├── twitter/route.js          # Twitter API v2 integration
│   │   ├── users/route.js            # إدارة المستخدمين VIP
│   │   ├── settings/route.js         # إعدادات النظام
│   │   ├── referral/route.js         # نظام الإحالة
│   │   └── ai/
│   │       ├── generate-posts/route.js  # توليد منشورات بـ Gemini AI
│   │       └── list-models/route.js     # جلب نماذج Gemini المتاحة
│   │
│   ├── components/                   # المكونات المشتركة
│   │   ├── FloatingChat.js           # زر الدعم العائم (تليجرام)
│   │   ├── EconomicCalendar.js       # التقويم الاقتصادي (Investing.com iframe)
│   │   ├── AINewsAnalysis.js         # تحليل الأخبار بالذكاء الاصطناعي
│   │   ├── BankAnalysis.js           # توقعات البنوك العالمية
│   │   └── COTReport.js              # تقرير التزام المتداولين
│   │
│   ├── context/
│   │   └── LanguageContext.js        # سياق اللغة (عربي/إنجليزي)
│   │
│   └── utils/
│       ├── translations.js           # ملف الترجمات (608 سطر)
│       └── blogData.js               # بيانات المدونة
│
├── models/                           # Mongoose Models
│   ├── Signal.js                     # نموذج التوصية
│   ├── User.js                       # نموذج المستخدم
│   └── Settings.js                   # نموذج الإعدادات
│
├── lib/
│   └── mongodb.js                    # اتصال MongoDB
│
└── public/                           # الملفات الثابتة
    ├── images/
    ├── blog-images/
    ├── cash.mp3, cash.wav            # أصوات
    ├── manifest.json                 # PWA manifest
    └── og-image.png                  # صورة المشاركة
```

---

## 📊 نماذج قاعدة البيانات (Models)

### 1. Signal.js - نموذج التوصية/المنشور
```javascript
{
  pair: String,                    // زوج العملة (مثل "GOLD")
  type: String,                    // "VIP" أو "REGULAR"
  imageUrl: String,                // الصورة الواضحة (الأصلية)
  blurredImageUrl: String,         // الصورة المموهة للـ VIP
  isVip: Boolean,                  // هل هي توصية VIP؟
  customPost: String,              // نص المنشور المخصص
  publishedToWebsite: Boolean,     // هل منشورة على الموقع؟
  telegramMessageId: String,       // معرف رسالة تليجرام
  telegramButtonType: String,      // نوع الزر: "view_signal" | "share" | "subscribe" | "none"
  twitterTweetId: String,          // معرف تغريدة تويتر
  createdAt: Date
}
```

### 2. User.js - نموذج المستخدم
```javascript
{
  telegramId: String,              // معرف تليجرام (فريد - unique)
  isVip: Boolean,                  // حالة VIP
  subscriptionEndDate: Date,       // تاريخ انتهاء الاشتراك (null = مدى الحياة)
  name: String,
  referralCode: String,            // كود الإحالة (8 أحرف عشوائية)
  referredBy: String,              // كود من أحاله
  referralCount: Number,           // عدد الإحالات الناجحة
  referralRewardsEarned: Number    // المكافآت المكتسبة
}
```

### 3. Settings.js - نموذج الإعدادات
```javascript
{
  geminiApiKey: String,            // مفتاح Gemini AI
  aiPrompt: String,                // قالب AI المخصص
  selectedModel: String,           // نموذج AI المختار (gemini-2.0-flash, etc.)
  generatedPostCount: Number,      // عدد المنشورات المولدة (افتراضي: 50)
  twitterApiKey: String,           // Twitter API credentials
  twitterApiSecret: String,
  twitterAccessToken: String,
  twitterAccessSecret: String
}
```

---

## 🔄 نظام النشر المتعدد (Multi-Platform Publishing)

### أنواع المنشورات:
| النوع | الوصف | السلوك |
|-------|-------|--------|
| **VIP** | توصية حصرية | تظهر مموهة لغير المشتركين في كل المنصات |
| **Regular** | توصية عادية | تظهر واضحة للجميع في كل المنصات |

### منصات النشر:
1. **الموقع** (`publishedToWebsite: true`)
2. **تليجرام** (`telegramMessageId` موجود)
3. **تويتر/X** (`twitterTweetId` موجود)

### منطق التمويه للـ VIP:
```javascript
// عند النشر:
if (signalType === 'vip') {
    // تليجرام: يتم إرسال blurredImageUrl (الصورة المموهة)
    // تويتر: يتم إرسال blurredImageUrl (الصورة المموهة)
    // الموقع: يتم حفظ كلا الصورتين
}

// عند العرض على الموقع:
if (user.isVip) {
    // عرض imageUrl (الصورة الواضحة)
} else {
    // عرض blurredImageUrl (الصورة المموهة)
}
```

### إنشاء الصورة المموهة (Frontend):
```javascript
const createBlurredImage = (file) => {
    // 1. تطبيق blur filter على الصورة
    ctx.filter = 'blur(10px)';
    
    // 2. إضافة أيقونة القفل الذهبية في المنتصف
    // SVG مع تدرج ذهبي وتأثير glow
    
    // 3. تحويل إلى base64 JPEG
    return canvas.toDataURL('image/jpeg', 0.85);
};
```

---

## 🔐 نظام VIP

### التحقق من صلاحية VIP:
```javascript
// في /api/signals GET
if (user && user.isVip) {
    if (user.subscriptionEndDate) {
        const now = new Date();
        const end = new Date(user.subscriptionEndDate);
        if (now > end) {
            // منتهي الصلاحية - إلغاء VIP تلقائياً
            user.isVip = false;
            user.subscriptionEndDate = null;
            await user.save();
        }
    }
    // إذا subscriptionEndDate = null فهو مدى الحياة ♾️
}
```

### أنواع الاشتراك:
| النوع | isVip | subscriptionEndDate |
|-------|-------|---------------------|
| محدد المدة | `true` | `Date` (تاريخ الانتهاء) |
| مدى الحياة | `true` | `null` |
| غير مشترك | `false` | `null` |

---

## 📱 لوحة تحكم الإدمن (Admin Panel)

### كلمة المرور: `123`

### التبويبات الأربعة:

#### 1. Publish (نشر) 📤
- **رفع الصورة**: سحب وإفلات، لصق من الحافظة (Ctrl+V)، أو اختيار ملف
- **نوع المنشور**: VIP (مموه) أو Regular (عادي)
- **نص المنشور**: textarea مع حفظ تلقائي في localStorage
- **توليد AI**: توليد نسخ متعددة من المنشور باستخدام Gemini
- **منصات النشر**: اختيار الموقع / تليجرام / تويتر
- **زر تليجرام**: view_signal / subscribe / share / none
- **وضع التعديل**: تعديل المنشورات الموجودة

#### 2. Signals (التوصيات) 📊
- عرض جميع التوصيات المنشورة
- شارات المنصات (🌐 Website, 📱 TG, 𝕏 Twitter)
- شارة VIP / Regular
- أزرار التعديل والحذف
- عرض الوقت النسبي (منذ X دقيقة/ساعة/يوم)

#### 3. VIP Management (إدارة VIP) 👑
- إضافة عضو VIP جديد بـ Telegram ID
- تحديد المدة (بالأشهر) أو مدى الحياة
- جدول الأعضاء النشطين مع:
  - Telegram ID
  - الحالة (Active)
  - الوقت المتبقي
  - زر الإزالة

#### 4. Settings (الإعدادات) ⚙️
- **إعدادات Gemini AI**:
  - API Key
  - اختيار النموذج (gemini-2.0-flash, gemini-1.5-flash, gemini-1.5-pro)
  - عدد النسخ المولدة
  - Custom Prompt
- **إعدادات Twitter/X**:
  - API Key & Secret
  - Access Token & Secret

---

## 🔗 API Endpoints

### `/api/signals`
| Method | الوظيفة | Parameters |
|--------|---------|------------|
| GET | جلب التوصيات | `?admin=true` للإدمن، `?telegramId=xxx` للمستخدم |
| POST | إنشاء توصية جديدة | `pair`, `type`, `imageUrl`, `telegramImage`, `sendToWebsite`, `sendToTelegram`, `sendToTwitter`, `isVip`, `customPost`, `telegramButtonType` |
| PUT | تعديل توصية | `id`, `customPost`, `telegramButtonType`, `type`, `isVip`, `updateWebsite`, `updateTelegram`, `updateTwitter`, `imageUrl` |
| DELETE | حذف توصية | `?id=xxx` |

### `/api/twitter`
| Method | الوظيفة |
|--------|---------|
| POST | نشر تغريدة جديدة (مع صورة اختيارية) |
| DELETE | حذف تغريدة |

### `/api/users`
| Method | الوظيفة |
|--------|---------|
| GET | جلب كل المستخدمين |
| POST | إضافة/تعديل/حذف مستخدم VIP |

### `/api/settings`
| Method | الوظيفة |
|--------|---------|
| GET | جلب الإعدادات |
| POST | حفظ الإعدادات |

### `/api/referral`
| Method | الوظيفة |
|--------|---------|
| GET | جلب بيانات الإحالة للمستخدم |
| POST | تسجيل إحالة جديدة |

### `/api/ai/generate-posts`
| Method | الوظيفة |
|--------|---------|
| GET | جلب الـ Default Prompt |
| POST | توليد نسخ من المنشور باستخدام Gemini |

### `/api/ai/list-models`
| Method | الوظيفة |
|--------|---------|
| POST | جلب نماذج Gemini المتاحة |

---

## 🌐 نظام اللغات (Bilingual Support)

### اللغات المدعومة:
- **العربية (ar)**: RTL، خط Cairo
- **الإنجليزية (en)**: LTR، خط Inter

### LanguageContext:
```javascript
const { t, lang, toggleLang, isRTL, testimonials, faqData, mounted } = useLanguage();

// t = كائن الترجمات للغة الحالية
// lang = "ar" أو "en"
// toggleLang = دالة تبديل اللغة
// isRTL = true للعربية
// testimonials = شهادات العملاء باللغة الحالية
// faqData = الأسئلة الشائعة باللغة الحالية
// mounted = للتأكد من تحميل الـ client
```

### ملف الترجمات (`translations.js`):
يحتوي على 608 سطر من الترجمات تشمل:
- نصوص الصفحة الرئيسية
- نصوص صفحة التوصيات
- نصوص لوحة الإدمن
- نصوص VIP Management
- نصوص AI Settings
- نصوص حاسبة اللوت
- نصوص FAQ
- نصوص Legal (Privacy, Terms, Risk)
- نصوص المدونة
- شهادات العملاء (testimonials)

---

## 🎨 التصميم والأنماط

### الألوان الرئيسية:
```css
--gold-primary: #B8860B;      /* الذهبي الأساسي */
--gold-medium: #DAA520;       /* الذهبي المتوسط */
--gold-dark: #705C0B;         /* الذهبي الداكن */
--bg-dark: #0a0a0a;           /* خلفية داكنة */
--bg-card: #111111;           /* خلفية البطاقات */
--text-primary: #ffffff;
--text-secondary: #b0b0b0;
```

### الأنماط المهمة:
```css
.text-gradient {
    background: linear-gradient(90deg, #FFD700, #FFE566, #FFFFFF, #FFE566, #FFD700);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: goldShine 3s linear infinite;
}

.btn-primary {
    background: linear-gradient(135deg, #B8860B 0%, #DAA520 50%, #B8860B 100%);
    /* تأثير hover مع transform و box-shadow */
}

.card {
    background: var(--bg-card);
    border: 1px solid rgba(184, 134, 11, 0.2);
    border-radius: 20px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.4);
}
```

---

## 🧩 المكونات (Components)

### 1. FloatingChat.js
زر دعم عائم يفتح قناة تليجرام للدعم.

### 2. EconomicCalendar.js
يعرض التقويم الاقتصادي من Investing.com عبر iframe مع:
- فلتر للأحداث عالية التأثير
- دعم اللغتين
- تصميم داكن متوافق

### 3. AINewsAnalysis.js
يعرض تحليل الأخبار بالذكاء الاصطناعي:
- Market Sentiment (صعودي/هبوطي/حيادي)
- AI Summary
- Top News مع Impact badges

### 4. BankAnalysis.js
يعرض توقعات البنوك العالمية:
- اسم البنك
- الأصل (Asset)
- السعر المستهدف
- الاتجاه (Bullish/Bearish)

### 5. COTReport.js
يعرض تقرير التزام المتداولين:
- نسبة العقود الشرائية (Longs)
- نسبة العقود البيعية (Shorts)
- الاتجاه العام

---

## 📄 الصفحات

### 1. الصفحة الرئيسية (`app/page.js`)
- Hero Section مع إحصائيات
- Profit Simulator (حاسبة الفرص الضائعة)
- Features Section
- Testimonials
- Pricing Plans (شهري/3 أشهر/سنوي)
- FAQ Accordion
- Risk Disclaimer
- Footer

### 2. صفحة التوصيات (`app/signals/page.js`)
- عرض التوصيات مع فلترة حسب VIP
- شارة VIP Status للمستخدم
- عرض الصور المموهة/الواضحة حسب الاشتراك

### 3. سجل الأداء (`app/track-record/page.js`)
- عرض سجل الأداء التاريخي
- إحصائيات النجاح

### 4. المدونة (`app/blog/page.js`)
- قائمة المقالات التعليمية
- صفحات المقالات الفردية

### 5. الصفحة القانونية (`app/legal/page.js`)
- Privacy Policy
- Terms of Service
- Risk Disclaimer

---

## 🔧 المتغيرات البيئية (.env.local)

```env
MONGODB_URI=mongodb+srv://...
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHANNEL_ID=@...
IMGBB_API_KEY=...
```

---

## 📝 ملاحظات تقنية مهمة

1. **كلمة مرور الإدمن**: `123` (ثابتة في الكود)
2. **رفع الصور**: يتم رفعها إلى ImgBB ثم حفظ الرابط في MongoDB
3. **التمويه**: يتم إنشاء صورة مموهة في الـ Frontend باستخدام Canvas API قبل الإرسال
4. **تويتر**: يستخدم Twitter API v2 مع OAuth 1.0a
5. **تليجرام**: يستخدم Bot API مع Inline Keyboards
6. **الـ Session**: يتم حفظ حالة تسجيل الدخول في `sessionStorage`
7. **الـ localStorage**: يحفظ نص المنشور المخصص للإدمن
8. **Gemini AI**: يدعم نماذج متعددة مع Custom Prompt

---

## 🔄 تدفق العمل (Workflow)

### نشر توصية جديدة:
1. الإدمن يرفع صورة (اختياري)
2. يختار نوع المنشور (VIP/Regular)
3. يكتب نص المنشور (اختياري)
4. يمكنه توليد نسخ بالـ AI
5. يختار منصات النشر
6. إذا اختار تليجرام، يختار نوع الزر
7. يضغط "نشر الآن"
8. الـ API:
   - يرفع الصورة إلى ImgBB
   - يحفظ في MongoDB
   - يرسل إلى تليجرام (إذا مختار)
   - يرسل إلى تويتر (إذا مختار)

### تعديل توصية:
1. الإدمن يضغط "تعديل" على توصية موجودة
2. يتم تحميل البيانات في نموذج النشر
3. يمكنه تغيير الصورة/النص/المنصات
4. يضغط "تحديث"
5. الـ API:
   - يحدث في MongoDB
   - يحذف/يضيف من المنصات حسب التغييرات
   - تويتر: يحذف القديمة وينشر جديدة (لا يدعم التعديل)

---

## 📅 آخر تحديث: ديسمبر 2024
